Show the options chain for $ARGUMENTS.

Use the `options_chain_summary` MCP tool with the given symbol.
Display: ATM strike, PCR (OI), max pain, total OI (calls vs puts).

If a specific expiry is mentioned, use that. Otherwise default to nearest weekly.
Highlight the ATM strike and any strikes with unusual OI concentration.
