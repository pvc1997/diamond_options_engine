Scan market for best strategies now.

1. Use `get_india_vix` for VIX
2. Use `get_spot_price` for NIFTY spot (or use synthetic)
3. Estimate IV rank and trend from available data
4. Use `scan_market_signals` to score all strategies
5. Show top 5 strategies with:
   - Score (0-100)
   - Confidence level
   - Category and risk profile
   - Key reasons for the score
6. For the top pick, show full setup with `suggest_strategy`

This is the "what should I trade today?" command.
