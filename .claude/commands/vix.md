Show VIX analysis.

1. Use `get_india_vix` for current VIX + regime
2. Use `vix_mean_reversion` for signal
3. Use `strategies_for_vix_regime` for suitable strategies
4. Display:
   - Current VIX and regime (low/normal/elevated/high/crisis)
   - Mean reversion signal (if any)
   - Position sizing multiplier
   - Suitable strategies for this regime
   - Strategy bias (sell premium / buy protection / hedge only)
5. Historical context if available
