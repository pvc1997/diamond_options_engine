Show the F&O stock universe.

Use the `fno_universe` MCP tool. If the user specifies a sector, filter by it.
Display: symbol, lot size, sector — sorted by sector.
Show total count and sector-wise breakdown.
