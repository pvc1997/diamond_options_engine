Show upcoming F&O expiry dates.

Use the `upcoming_expiries` MCP tool to show the next 12 expiries.
Include: date, label, type (current_week/next_week/current_month/far_month), days remaining.

Also check `is_expiry_today` and alert if today is expiry day.
